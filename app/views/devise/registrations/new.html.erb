<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <div class="well">
      <% if params[:plan] == '2' %>
        <h1 class="text-center">Paying It Forward</h1>
        <%= render 'donate_form' %>
      <% else %>
        <h1 class="text-center">A Helping Community</h1>
        <%= render 'signup_form' %>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
